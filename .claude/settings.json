{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "cd /home/lucas/GestaoEsc && git add -A && git diff --cached --quiet || (git commit -m \"auto: update $(date '+%Y-%m-%d %H:%M:%S')\" && git push) 2>/dev/null || true",
            "statusMessage": "Sincronizando com GitHub..."
          }
        ]
      }
    ]
  }
}
